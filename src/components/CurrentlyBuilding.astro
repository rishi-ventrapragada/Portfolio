---
// PRD §5.4, partial. The marquee and the src/content/now.json source are
// deferred; this increment ships a single static line. Copy is hardcoded
// deliberately — a one-entry JSON collection would be overhead with no payoff
// until the marquee lands.
---

<section class="now shell" aria-labelledby="now-heading">
  <h2 id="now-heading" class="label heading">Currently building</h2>
  <p class="line">
    Recurzn &mdash; a cross-platform life tracker with a voice assistant.
  </p>
</section>

<style>
  .now {
    padding-block: var(--rhythm);
    border-top: 1px solid var(--line);
  }

  .heading {
    margin: 0 0 1rem;
    color: var(--accent);
  }

  .line {
    margin: 0;
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    color: var(--fg);
  }
</style>
