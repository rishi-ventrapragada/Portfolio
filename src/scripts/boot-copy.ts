/**
 * Boot preloader copy (PRD §5.10).
 *
 * Deliberately its own module: an .astro frontmatter export is not importable,
 * so keeping the string here is what makes swapping it a genuine one-line
 * change with a single place to look.
 */

/** [TODO] Placeholder — final copy deferred pending the owner's decision. */
export const STATUS_TEXT = "LOADING";
